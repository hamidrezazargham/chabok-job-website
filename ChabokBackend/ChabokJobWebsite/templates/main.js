$('.slider').slick({
    autoplay:true,
    infinite: true,
    speed: 300,
    slidesToShow: 3,
    centerMode: true,
    centerPadding: '60px',
    variableWidth: true ,
  });

$('.job-title').aofsr({
  phraseMode: true
});
